org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin incremental compilation fails on this project ("Could not close
# incremental caches ... class-fq-name-to-source.tab") and survives both a cache
# wipe and flutter clean. Release builds do not benefit from it anyway; leaving
# it on costs a broken build on the one afternoon that cannot absorb it.
kotlin.incremental=false
